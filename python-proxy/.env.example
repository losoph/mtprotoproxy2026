# Copy to .env (git-ignored) and fill in. Generate a secret with:
#   openssl rand -hex 16
MTPROTO_SECRET=
# Optional overrides:
# MTPROTO_PORT=8443
# MTPROTO_TLS_DOMAIN=avito.ru
# MTPROTO_ALERT_EMAIL=you@example.com
